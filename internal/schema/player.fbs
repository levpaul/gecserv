namespace player;

enum Color:byte { Red = 0, Green, Blue, Yellow, Pink, Gray, Orange}
enum UpdateMsg:byte { login = 0, logout, sync }

struct Vec2 {
  x:float;
  y:float;
}

table Player{
  pos: Vec2;
  uuid: string;
  col: Color;
  updateMsg: UpdateMsg;
}

root_type Player;
